{
  "name": "quizly",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:db": "json-server --port 5000 -w db.json",
    "start:client": "npm start --prefix client",
    "start": "concurrently npm:start:*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.2.1",
    "json-server": "^0.16.3"
  }
}
